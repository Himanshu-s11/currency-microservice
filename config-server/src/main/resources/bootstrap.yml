spring:
  application:
       name: config-server
server:
  port: 3333

  # Git Repo URL


  cloud:
    config:
       server:
          git:
            uri: https://github.com/Himanshu-s11/microservice-config.git
            clone-on-start: true